import React from "react";
import Router from "./Router";
import { Container } from "@mui/material";

const App: React.FC = () => {
  return (
      <Container sx={{ mt: 4 }}>

  <Router />
  </Container>

  )
};

export default App;
